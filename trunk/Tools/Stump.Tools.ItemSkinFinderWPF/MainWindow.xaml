<Window x:Class="Stump.Tools.ItemSkinFinderWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded">
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="32"/>
      <RowDefinition/>
      <RowDefinition Height="35"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <Button Content="NEXT" Margin="5" Command="{Binding NextCommand}"/>
    <Button Grid.Column="1" Content="PROCESS ALL" Margin="5" Command="{Binding ProcessAllCommand}"/>
    <Image Grid.Row="1" Grid.Column="0" Source="{Binding ImageA}" Margin="5" Stretch="None"/>
    <Image Grid.Row="1" Grid.Column="1" Source="{Binding ImageB}" Margin="5" Stretch="None"/>
    <ProgressBar Grid.Row="2" Grid.ColumnSpan="2" Minimum="0" Maximum="100" Margin="5,5,5,0" Value="{Binding Value}"/>
    <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Text="{Binding StringFormat='{}{0:0.0}%', Path=Value}" TextAlignment="Center" HorizontalAlignment="Stretch"/>
  </Grid>
</Window>
