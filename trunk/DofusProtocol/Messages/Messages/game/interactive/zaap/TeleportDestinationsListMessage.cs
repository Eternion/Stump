// File generated by 'DofusProtocolBuilder.exe v1.0.0.0'
// From 'TeleportDestinationsListMessage.xml' the '15/06/2011 01:39:01'
using System;
using Stump.BaseCore.Framework.IO;

namespace Stump.DofusProtocol.Messages
{
	public class TeleportDestinationsListMessage : Message
	{
		public const uint Id = 5960;
		public override uint MessageId
		{
			get
			{
				return 5960;
			}
		}
		
		public byte teleporterType;
		public int[] mapIds;
		public short[] subareaIds;
		public short[] costs;
		
		public TeleportDestinationsListMessage()
		{
		}
		
		public TeleportDestinationsListMessage(byte teleporterType, int[] mapIds, short[] subareaIds, short[] costs)
		{
			this.teleporterType = teleporterType;
			this.mapIds = mapIds;
			this.subareaIds = subareaIds;
			this.costs = costs;
		}
		
		public override void Serialize(IDataWriter writer)
		{
			writer.WriteByte(teleporterType);
			writer.WriteUShort((ushort)mapIds.Length);
			for (int i = 0; i < mapIds.Length; i++)
			{
				writer.WriteInt(mapIds[i]);
			}
			writer.WriteUShort((ushort)subareaIds.Length);
			for (int i = 0; i < subareaIds.Length; i++)
			{
				writer.WriteShort(subareaIds[i]);
			}
			writer.WriteUShort((ushort)costs.Length);
			for (int i = 0; i < costs.Length; i++)
			{
				writer.WriteShort(costs[i]);
			}
		}
		
		public override void Deserialize(IDataReader reader)
		{
			teleporterType = reader.ReadByte();
			if ( teleporterType < 0 )
			{
				throw new Exception("Forbidden value on teleporterType = " + teleporterType + ", it doesn't respect the following condition : teleporterType < 0");
			}
			int limit = reader.ReadUShort();
			mapIds = new int[limit];
			for (int i = 0; i < limit; i++)
			{
				mapIds[i] = reader.ReadInt();
			}
			limit = reader.ReadUShort();
			subareaIds = new short[limit];
			for (int i = 0; i < limit; i++)
			{
				subareaIds[i] = reader.ReadShort();
			}
			limit = reader.ReadUShort();
			costs = new short[limit];
			for (int i = 0; i < limit; i++)
			{
				costs[i] = reader.ReadShort();
			}
		}
	}
}
