// File generated by 'DofusProtocolBuilder.exe v1.0.0.0'
// From 'ObjectEffects.xml' the '14/06/2011 11:32:48'
using System;
using Stump.Core.IO;

namespace Stump.DofusProtocol.Types
{
	public class ObjectEffects
	{
		public const uint Id = 358;
		public short TypeId
		{
			get
			{
				return 358;
			}
		}
		
		public short powerRate;
		public bool overMax;
		public Types.ObjectEffect[] effects;
		
		public ObjectEffects()
		{
		}
		
		public ObjectEffects(short powerRate, bool overMax, Types.ObjectEffect[] effects)
		{
			this.powerRate = powerRate;
			this.overMax = overMax;
			this.effects = effects;
		}
		
		public virtual void Serialize(IDataWriter writer)
		{
			writer.WriteShort(powerRate);
			writer.WriteBoolean(overMax);
			writer.WriteUShort((ushort)effects.Length);
			for (int i = 0; i < effects.Length; i++)
			{
				writer.WriteShort(effects[i].TypeId);
				effects[i].Serialize(writer);
			}
		}
		
		public virtual void Deserialize(IDataReader reader)
		{
			powerRate = reader.ReadShort();
			overMax = reader.ReadBoolean();
			int limit = reader.ReadUShort();
			effects = new Types.ObjectEffect[limit];
			for (int i = 0; i < limit; i++)
			{
				effects[i] = ProtocolTypeManager.GetInstance<Types.ObjectEffect>(reader.ReadShort());
				effects[i].Deserialize(reader);
			}
		}
	}
}
