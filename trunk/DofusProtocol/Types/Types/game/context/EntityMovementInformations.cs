// File generated by 'DofusProtocolBuilder.exe v1.0.0.0'
// From 'EntityMovementInformations.xml' the '30/06/2011 11:40:22'
using System;
using Stump.Core.IO;
using System.Collections.Generic;
using System.Linq;

namespace Stump.DofusProtocol.Types
{
	public class EntityMovementInformations
	{
		public const uint Id = 63;
		public virtual short TypeId
		{
			get
			{
				return 63;
			}
		}
		
		public int id;
		public IEnumerable<byte> steps;
		
		public EntityMovementInformations()
		{
		}
		
		public EntityMovementInformations(int id, IEnumerable<byte> steps)
		{
			this.id = id;
			this.steps = steps;
		}
		
		public virtual void Serialize(IDataWriter writer)
		{
			writer.WriteInt(id);
			writer.WriteUShort((ushort)steps.Count());
			foreach (var entry in steps)
			{
				writer.WriteByte(entry);
			}
		}
		
		public virtual void Deserialize(IDataReader reader)
		{
			id = reader.ReadInt();
			int limit = reader.ReadUShort();
			steps = new byte[limit];
			for (int i = 0; i < limit; i++)
			{
				(steps as byte[])[i] = reader.ReadByte();
			}
		}
	}
}
